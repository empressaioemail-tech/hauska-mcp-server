# Social posts — drafts

DRAFT. Do not publish; operator-gated.

## X / Twitter

**Launch post:**

```
We just opened the Hauska MCP Server — a free, public MCP endpoint that
gives AI agents cited answers about municipal building code and zoning.

Every result is an atom: a stable id + content hash + source document.
Agents cite the code, they don't paraphrase it.

mcp.hauska.dev
```

**Follow-up (thread, optional):**

```
Why it matters: an agent doing permitting or zoning work that guesses a
setback rule is a liability. One that retrieves the section and cites it
is a tool you can trust.

Works in Claude Desktop, Claude Code, Cursor, or any MCP client.
Quickstarts + a runnable example: mcp.hauska.dev/docs
```

## LinkedIn

```
Opening the Hauska MCP Server today.

AI agents are moving into real work on the built world — permitting,
zoning analysis, plan pre-checks. The gap is not capability, it is
grounding. An agent that paraphrases a code rule is guessing; one that
cites the section is working.

The Hauska MCP Server is a public Model Context Protocol endpoint that
gives any agent jurisdiction-grounded answers about municipal building
codes, with a source citation on every result. Every answer is an atom
with a stable id, a content hash, and its source document. The public
catalog is free.

If you are building construction-tech, permitting, or real-estate agents,
it drops in over MCP: Claude Desktop, Claude Code, Cursor, or a custom
SDK agent.

Docs and quickstarts: mcp.hauska.dev/docs
```

> [GTM-SESSION] Confirm the posting accounts, the launch-day sequencing
> relative to Show HN and ProductHunt, and any coverage stat to include.
