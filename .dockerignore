# Keep the build context small and deterministic. The Dockerfile copies
# package.json, package-lock.json, tsconfig.json, src/, scripts/, and
# docs/content/; the docs site (docs/site/) is generated inside the build.
node_modules
dist
docs/site
.git
.github
.env
.env.example
pnpm-lock.yaml
npm-debug.log*
tests
examples
launch
_research
_sessions
observability
deploy
README.md
.DS_Store
